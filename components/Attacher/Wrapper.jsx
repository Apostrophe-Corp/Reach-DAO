import React from "react";

const AttacherWrapper = ({ children }) => {
    return (
        <div>
            <h1>Attacher</h1>
            { children }
        </div>
    );
};

export default AttacherWrapper;